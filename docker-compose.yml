services:
  # --- CORE SERVICES ---
  db:
    image: postgres:15-alpine
    container_name: laundry_db
    restart: always
    ports:
      - "5432:5432"
    env_file: .env  # Mengambil POSTGRES_USER, dsb dari root .env
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 5
    environment:
      - TZ=Asia/Jakarta
    networks:
      - app-bridge

  redis:
    image: redis:alpine
    container_name: laundry_redis
    restart: always
    environment:
      - TZ=Asia/Jakarta
    networks:
      - app-bridge

  adminer:
    image: adminer
    container_name: laundry_adminer
    restart: always
    env_file: .env
    depends_on:
      - db
    networks:
      - app-bridge

  # --- BACKEND SERVICES ---
  backend-dev:
    profiles: ["dev"]
    build:
      context: ./backend
      dockerfile: ./builder/local/Dockerfile
    container_name: laundry_api_dev
    volumes:
      - ./backend:/app
    env_file: .env  # Menyuntikkan SEMUA variabel .env (SECRET_KEY, dsb) ke dalam FastAPI
    environment:
      - REDIS_URL=redis://redis:6379/0
      - ENV=development
      - TZ=Asia/Jakarta
    depends_on:
      db:
        condition: service_healthy
    networks:
      - app-bridge

  backend-prod:
    profiles: ["prod"]
    build:
      context: ./backend
      dockerfile: ./builder/prod/Dockerfile
    container_name: laundry_api_prod
    restart: always
    volumes:
      - backend_media_data:/app/uploads
    env_file: .env
    environment:
      - REDIS_URL=redis://redis:6379/0
      - ENV=production
      - TZ=Asia/Jakarta
    depends_on:
      db:
        condition: service_healthy
    networks:
      - app-bridge

  # --- FRONTEND SERVICES ---
  frontend-dev:
    profiles: ["dev"]
    build:
      context: ./frontend
      dockerfile: ./builder/local/Dockerfile
    container_name: laundry_ui_dev
    volumes:
      - ./frontend:/app
      - /app/node_modules # Mencegah konflik node_modules lokal & container (anonymous volume)
      - /app/.vite # Cache Vite untuk performa lebih baik
    ports:
      - "5173:5173" # Expose port langsung untuk akses langsung ke Vite dev server
    environment:
      # PENTING: Memaksa Vite/Chokidar mendeteksi perubahan file di Docker
      - CHOKIDAR_USEPOLLING=true
      - WATCHPACK_POLLING=true
      - NODE_ENV=development
      - VITE_HMR_PORT=5173
      # Optimasi Node.js untuk performa
      - NODE_OPTIONS=--max-old-space-size=4096
    env_file: .env
    # Healthcheck untuk memastikan container siap
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:5173"]
      interval: 5s
      timeout: 3s
      retries: 5
      start_period: 10s
    networks:
      - app-bridge
    # Restart policy untuk development
    restart: unless-stopped

  frontend-prod:
    profiles: ["prod"]
    build:
      context: ./frontend
      dockerfile: ./builder/prod/Dockerfile
      args:
        - VITE_API_URL=${VITE_API_URL}
    container_name: laundry_ui_prod
    restart: always
    env_file: .env
    networks:
      - app-bridge

  # --- NGINX GATEWAY ---
  nginx-dev:
    profiles: ["dev"]
    build:
      context: ./nginx
      dockerfile: ./builder/local/Dockerfile
    container_name: laundry_gateway_dev
    ports:
      - "80:80"
    depends_on:
      frontend-dev:
        condition: service_healthy
      backend-dev:
        condition: service_started
    networks:
      - app-bridge

  # nginx-prod:
  #   profiles: ["prod"]
  #   build:
  #     context: ./nginx
  #     dockerfile: ./builder/prod/Dockerfile
  #   container_name: laundry_gateway_prod
  #   ports:
  #     - "80:80"
  #     - "443:443"
  #   restart: always
  #   depends_on:
  #     - frontend-prod
  #     - backend-prod
  #   networks:
  #     - app-bridge

networks:
  app-bridge:
    external: true

volumes:
  postgres_data:
  backend_media_data: